# 项目风险复盘

## 背景

本项目计划在四周内上线知识库问答功能，目标是支持文档上传、向量检索、引用来源展示和聊天内选择知识库。

## 主要风险

1. Embedding API 配额不足可能导致上传大文件时失败。
2. PDF 和 DOCX 解析结果不稳定，复杂排版可能丢失表格结构。
3. chunk 太短会丢上下文，chunk 太长会降低召回精度。
4. 用户删除文档后，历史回答的引用快照需要继续可读。

## 缓解措施

首版限制单次上传文件总大小，失败时保留文档级错误信息。检索默认 top 5，并在 prompt 中要求模型在上下文不足时说明不确定。
